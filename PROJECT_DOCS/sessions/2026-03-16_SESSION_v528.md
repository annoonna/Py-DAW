# 📝 SESSION LOG: 2026-03-16 (v0.0.20.528)

**Entwickler:** Claude Opus 4.6
**Task:** Send-Automation — Send-Amount als automatierbarer Parameter

## ERLEDIGTE TASKS
- [x] Send-Amount als automatierbarer Parameter im AutomationManager registriert
- [x] AutomationPlaybackService liest dynamische Send-Lanes bei Playback
- [x] apply_automation_value() erweitert für send:{fx_track_id} Parameter
- [x] Send-Knob folgt Automation-Kurve (parameter_changed → blockSignals)
- [x] "Show Automation in Arranger" im Send-Kontextmenü

## CODE-ÄNDERUNGEN

**Geändert:**
- `pydaw/ui/mixer.py` (+60 Zeilen): `_register_send_automation()`, `_on_send_automation_changed()`, Kontextmenü erweitert
- `pydaw/services/project_service.py` (+15 Zeilen): `apply_automation_value()` Send-Branch
- `pydaw/services/automation_playback.py` (+18 Zeilen): Dynamische Send-Lane-Erkennung

**Neu:**
- `CHANGELOG_v0.0.20.528_SEND_AUTOMATION.md`

## ARCHITEKTUR-NOTIZEN

Die Send-Automation nutzt zwei parallele Pfade (genau wie Volume/Pan):

1. **AutomationManager (neuer Store):** Parameter registriert als `trk:{tid}:send:{fx_id}`.
   - tick() interpoliert Lane → parameter_changed Signal → Knob-Update
   - Pencil-Tool im Arranger zeichnet in AutomationManager._lanes
   - MIDI CC Recording schreibt in AutomationManager._lanes

2. **AutomationPlaybackService (Legacy Store):** Scannt `project.automation_lanes[tid]`
   - Erkennt dynamisch alle Keys die mit "send:" beginnen
   - _interp() → apply_automation_value() → track.sends[].amount

Beide Pfade konvergieren auf `_emit_updated()` → `AudioEngine.rebuild_fx_maps()` → Audio-Callback.

## NÄCHSTE SCHRITTE
1. **Device-Container (Schritt B)** — FX-Layer/Instrument-Layer/Chain (groß)
2. **Sidechain-Input von FX-Track** (klein)
3. **Send-Automation MIDI Learn** — MIDI-Controller direkt auf Send-Knob mappen
