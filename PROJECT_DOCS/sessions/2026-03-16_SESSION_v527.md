# 📝 SESSION LOG: 2026-03-16 (v0.0.20.527)

**Entwickler:** Claude Opus 4.6
**Zeit:** Session-Dauer ~15 min
**Tasks:** Send Pre/Post Toggle + FX-Spuren vor Master

## ERLEDIGTE TASKS
- [x] Send Pre/Post Toggle im Mixer — Rechtsklick-Kontextmenü auf Send-Knobs
- [x] FX-Spuren immer direkt vor Master positionieren (Bitwig-Style)

## CODE-ÄNDERUNGEN

**Geändert:**
- `pydaw/ui/mixer.py` (+55 Zeilen): `_on_send_context_menu()`, CustomContextMenu auf QDial
- `pydaw/services/project_service.py` (+20 Zeilen): `toggle_send_pre_fader()`, `_rebuild_track_order()` FX-Sortierung

**Neu:**
- `CHANGELOG_v0.0.20.527_SEND_PREPOST_FX_ORDER.md`

## TIEFENANALYSE

### Codebase-Zustand v0.0.20.526
- ~526 Versionen seit v0.0.20.0
- Projekt hat funktionierendes Send-/Return-System seit v518
- Audio-Engine verarbeitet pre_fader bereits korrekt im Callback (hybrid_engine.py)
- Send-Bus-Map wird atomar via `set_send_bus_map()` gepusht
- Alle 6 externe Plugin-Formate (VST2, VST3, LV2, LADSPA, DSSI, CLAP) voll gehostet
- SmartDrop-System hat umfangreichen Morph-Guard (v482-v515)
- AETERNA Synth sehr umfangreich (Phase 3b+)
- Notation-Editor mit Schlüsseln, Kontextmenü, Playhead

### Architektur-Beobachtungen
- ProjectService ist die zentrale API für alle Track-/Send-Operationen
- `_rebuild_track_order()` wird bei JEDER Spuränderung aufgerufen → perfekter Ort für FX-Sortierung
- Send-Daten fließen: ProjectService → project.tracks[].sends → AudioEngine.rebuild_fx_maps() → HybridAudioCallback._send_bus_map
- pre_fader war bereits im Datenmodell und Audio-Engine, fehlte nur die UI-Umschaltung

## PROBLEME
- Keine

## NÄCHSTE SCHRITTE (Empfehlung)
1. **Send-Automation** — Send-Amount als automatierbarer Parameter registrieren (mittel)
2. **Device-Container (Schritt B)** — FX-Layer/Instrument-Layer/Chain (groß, ~500+ Zeilen)
3. **Sidechain-Input** — Optional Sidechain aus Send-Signal ableiten (klein)
